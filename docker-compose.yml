version: "3"
services:
  api:
    image: certi-test-dev-soft
    build: .
    networks:
      - backend
    ports:
      - "8000:8000"

  web-cli:
    image: certi-test-dev-soft
    networks:
      - backend
    command: sh

networks:
  backend:
    driver: bridge
